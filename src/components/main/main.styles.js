import styled from 'styled-components';

const StyledMain = styled.div`
	width: 80vw;
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 20px;
	margin-inline: auto;
`;

export { StyledMain };
